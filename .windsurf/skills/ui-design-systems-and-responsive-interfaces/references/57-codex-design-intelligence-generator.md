# Design Intelligence Workflow Compatibility Alias

This file is kept so upstream reference-pack inventories map cleanly. For the wf-core Windsurf/Devin surface, use `57-design-intelligence-workflow.md`.

The wf-core package does not expose a design-intelligence CLI command. Build the packet manually from repo evidence, `data/design_intelligence_catalog.json`, and the UI reference files.
