---
auto_execution_mode: 0
description: Start a wf-core guided Windsurf task with routing, token-saving, and validation discipline
---

You are starting a wf-core managed task.

1. Restate the working brief, explicit requirements, constraints, and non-goals.
2. Choose one primary skill from `.windsurf/skills/` if the task is domain-specific.
3. For noisy commands, use `wf-core run -- <command>` before raw output is produced.
4. For existing-source edits, run the preserve-existing-flow checklist first.
5. Plan small batches, implement, validate, fix, and reconcile requirements before final response.
